Authentication & Permissions:
- Token authentication is enabled using DRF's TokenAuthentication.
- Default permissions are IsAuthenticated, so only authenticated users can access the API.
- Token retrieval endpoint: /api/api-token-auth/
My first Django project for ALX Django LearnLab.
